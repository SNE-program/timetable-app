# 更新日志

课表助手（app.timetable.mobile）的版本改动记录。

- 应用内也能看到同样的内容：**设置 → 关于 → 更新日志**（数据源为 `src/app/changelog.ts`）。
- 更详细的技术记录在 `docs/` 目录里，按版本分篇。
- 发布约定：每完成一版，先在这里追加一条，再同步 `src/app/changelog.ts` 与 `package.json` / `build.gradle` 的版本号。

---

## v1.5.0

电脑上也能好好用了 · 下载页

这一版只做两件事：把网页版在电脑上从「拉宽的手机」变成真正的桌面布局；把「去哪装 App」变成一条明确的路径。
手机端（含 Android 应用）一行样式、一个交互都没改。

**① 桌面版式：换一套外壳，而不是把手机布局拉宽**

| | 手机 | 电脑（≥1024px） |
| --- | --- | --- |
| 外壳 | 上顶栏 + 内容 + 下标签栏 | `grid-template-columns: 236px 1fr` —— 左导航 + 右内容 |
| 导航 | 底部五格 | 左侧竖排：品牌块 / 五个入口 / 版本号与「获取 Android 版」 |
| 课表 | 行高 68px、节次轴 44px | 行高 92px、节次轴 58px，横向铺满 |
| 设置 / 外观 | 单列 | `columns: 380px` 自动分列，面板 `break-inside: avoid` |
| 弹层 | 底部抽屉（88vh）| 屏幕正中的对话框（660px，说明书 960px，垂直水平都居中）|
| 交互 | 触控 | 悬停反馈、可见焦点框；← → 翻周、1–5 切页、/ 搜索 |

**为什么不写成纯 CSS 媒体查询**：同一份代码在 Android 壳里也跑，平板横屏同样超过 1024px，
而那种情况必须保留手机版式。媒体查询分不出「电脑」和「安卓平板」，所以判断放在 `src/ui/useWideLayout.ts`：
**宽度 ≥1024px 且不是原生壳**，命中就给根节点挂 `.app.wide`，整套桌面样式都写在这个前缀下。

**② 分列要加在里面那一层**

第一版把 `columns` 加在 `.content-inner.two-col` 上，实测 `panels=1 cols=1` ——
设置页的结构是 `.content-inner > div（视图根）> .panel…`，外层只有一个子元素，它自己就是一列，永远不会分栏。
改到 `> *` 之后实测 `panels=17 cols=2`（外观页 `panels=9 cols=2`）。
这类「看起来写了、其实没生效」的样式只能靠量出来，所以自检里新增了 `DESKTOP` 一行。

**③ 下载页**

地址 `/download/`（源文件 `public/download/index.html`），**不经过 React 的静态页**：
同学拿到的是链接，这一页应该立刻给出安装包与安装步骤，而不是先等 650KB 的应用包加载完、再让人自己去设置里找。

- 应用内所有「获取 Android 版」都指向这一页（`meta.ts` 的 `DOWNLOAD_PAGE`），它随产物发布，任何构建里都存在，不会有死链；
- 页上的下载按钮指向 `releases/latest/download/timetable-app.apk`：**发版时同时上传这个固定名字的附件**，这个地址才永远是最新版；
  版本化的 `timetable-app-vX.Y.Z.apk` 用来留档（GitHub 会去掉附件名里的非 ASCII 字符，所以附件名只能是英文）；
- 页面上写了当前版本、文件大小与 SHA-256，以及一行校验命令。

**④ 验证**

| 项 | 结果 |
| --- | --- |
| 桌面 1280 / 1440 / 1920 | `OVERFLOW 0`、`SMALL_TAP 0`、`CLIPPED 0`、`MARKDOWN 0`、`ROOT ok`；导航栏 236px 贴左、内容区从 x=236 起；课表网格 1034 / 1194 / 1674px 宽；行高变量 92px |
| 桌面课表 | `WRAP 0` —— 宽屏下课程名不再折行（手机 320px 上是 12 条，属既有现象）|
| 桌面设置 / 外观 | `panels=17 cols=2` / `panels=9 cols=2`，无溢出 |
| 桌面说明书弹层 | 960×1040，在 1280 宽视口里 left=160、垂直居中（792+520=1312=2624/2）|
| 手机 320 / 360 / 390 | 与上一版一致：`OVERFLOW 0`、`SMALL_TAP 0`、标签栏 5 项全在框内、`DESKTOP off` |
| 平板 1023px | `DESKTOP off` —— 阈值下沿保持手机版式 |
| 单元测试 | 455 通过 / 23 文件 |
| 类型检查 | `tsc --noEmit` 0 错误 |

顺带修了自检本身的一个假阳性：`panel-desc` 与 `check-how` 是说明性段落，
`ALLOW_WRAP` 里原来只写了 `desc`，于是面板变窄之后它们被算成「本该一行放下却折行」—— 这类段落本来就该折行。

**⑤ 交付**

| 项目 | 结果 |
| --- | --- |
| 版本 | versionCode **51** / versionName **1.5.0** |
| 新增文件 | `src/ui/useWideLayout.ts`、`public/download/index.html` |
| 网页版 | 由 Actions 从 `main` 构建发布 |
| Android | `课表助手-v1.5.0.apk` · 9.21 MB · SHA-256 `B48ACEA9CAA5368787D1C7BE7F405C4FEE5D93E7FE29409C36A07D1D4FD09973` · Defender `found no threats` |
| 签名 | 与前几版同一张证书，可直接覆盖安装 |

---
## v1.4.0

网页版：同一套代码，两个宿主

Android 版把该做的都做了，但分享门槛高：要传 APK、要允许未知来源，iOS 与电脑上根本装不了。
同学之间传一份课表的成本，比功能本身更影响它有没有用 —— 所以这一版把整个前端发布到 GitHub Pages，
点开一个链接就能用。**底线没变：仍然不联网、不注册、不上传。**

**① 迁移判据：这项能力是不是依赖原生壳**

| 类别 | 内容 | 处理 |
| --- | --- | --- |
| 纯计算 / 纯界面 | 时间引擎、周次、调停补课、无限时间轴、本周 / 今日 / 任务 / 出勤 / 搜索 / 课程详情 | 原样可用 |
| 自带的编码管线 | Excel / CSV 导入（自写 ZIP + `DecompressionStream`）、ICS / CSV / Markdown / 长图 / JSON / 主题包 / 角色包 / 分享码 | 原样可用，导出改走浏览器下载 |
| Canvas 与 CSS 动画 | 外观 DIY、角色（视频抽帧、走动、翻面、快捷菜单） | 原样可用 |
| 通知 | `CapacitorNotifier`（系统闹钟）→ `WebNotifier`（页面内定时器 + 系统通知） | 已存在，这一版把它接进设置页与说明书 |
| 系统能力 | 桌面小组件、电池优化白名单、自启动、精确闹钟授权、8 项真机检查 | **删掉，换成说明** |

**② 最难的不是删代码，是不让网页版继承 Android 的承诺**

「息屏也会提醒」「关掉应用照样响」在网页版是假话。用户信了，就会在某天早上错过一节课，然后卸载。
所以这一版把所有平台相关的说法都改成按 `isNativePlatform()` 当场分支：

- **首次启动**最后一条：Android 写「提醒交给系统闹钟」，网页版写「提醒需要页面保持打开」，并直接给出"要关掉也响就装 Android 版"；
- **设置页**：Android 的「后台提醒可靠性」整块换成 `WebReminderPanel`「网页版的提醒边界」（页面要开着 / 后台标签页会被限速 / 数据不跨设备）；「精确闹钟」行在网页版不渲染；
- **桌面小组件**面板：网页版换成"为什么没有"+ 构建注入了仓库地址时的 Android 版入口；
- **提醒可靠性检查**：8 项换成 3 项（通知权限 / 页面开着时是否准时 / 关掉页面就不会响），报告里的字段也按平台换（网页版不再出现电池与厂商）；
- **说明书**：提醒章节的「必须放行的四项」在网页版变成「四条边界」，FAQ 的提醒、导出位置、小组件、换机迁移各有一份网页版答案，数据与存储章节写明 5 MB 配额；
- **更新日志**顶部加平台提示：历史条目里的系统闹钟 / 小组件 / 电池优化属于 Android 版 —— 记录不改写，但读者要知道自己在看什么。

**③ 发布**

`base` 改成 `'./'`：GitHub Pages 的项目页在子路径下，绝对路径 `/assets/…` 会指到域名根目录、整站白屏；
相对路径在子路径、自定义域名、以及 Capacitor 的 `https://localhost/` 下都成立，于是**两个平台共用一份构建产物**。

新增 `.github/workflows/web.yml`：推送 `main` → `npm ci` → `npm test` → `npm run build` → `deploy-pages`。
单测不过就不发布。仓库设置里把 Pages 的 Source 选成 GitHub Actions 即可。

网页版还需要一个"去哪装 Android 版"的入口，但地址不能靠猜。做法是构建时注入：
Actions 自带 `GITHUB_REPOSITORY`，`vite.config.ts` 把它折成 `__REPO_URL__`，
没配置时 `ANDROID_RELEASE_URL` 为空、所有入口自动隐藏 —— **宁可少一个按钮，也不要一个打不开的死链**。

**④ 验证：怎么确认"渲染出来的真是网页版那一支"**

平台分支是运行时决定的，静态检查看不出来。所以在自检里加了 `PLATFORM` 指标与 `?text=<选择器>`：
把匹配元素的**渲染后文字**用图片信标回传，直接读界面上写了什么。

把 `dist/` 挂到 `/repo/` 前缀下（模拟 Pages 子路径）用 Edge 无头跑：

| 检查 | 结果 |
| --- | --- |
| 子路径资源 | 全部 200，无 404；`index.html` 引用 `./assets/…` |
| 320 / 360 / 390 三宽度 | `OVERFLOW 0`、`SMALL_TAP 0`、`MARKDOWN 0`、`ROOT ok`、标签栏 5 项全在框内 |
| `PLATFORM` | `web` |
| 首次启动说明 | 4 条，最后一条为网页版文案 |
| 提醒可靠性检查 | `.check-row n=3`（Android 为 8） |
| 说明书四章 | 提醒 / 数据与存储 / FAQ / 版本与帮助 均为网页版分支，无裸 `**` |
| 更新日志顶部 | 平台提示正确出现 |
| Android 下载入口 | 注入 `VITE_REPO_URL` 后 3 个 `a[href]`；不注入时 0 个 |

**⑤ 还没做的（都在 docs/11 里写清了理由）**

- IndexedDB 资产后端 —— 网页版图片与结构化数据共用 localStorage 的约 5 MB 配额，这是最实际的痛点；
- PWA / 离线 —— 快速发版时用户会拿到旧版本，先发稳再谈；
- 网页版的后台提醒 —— 浏览器不给网页这个机会（Web Push 需要服务器，与「不联网」冲突），所以不做，只在界面上说清楚。

**⑥ 交付信息**

- 网页版：由 GitHub Actions 从 `main` 构建并发布，产物与 APK 内的前端同源同版本；
- Android：`课表助手-v1.4.0.apk`，`versionCode 50`，与 1.3.0 同一签名（证书 SHA-256 `a1c36fc4…dc25a`）；
- 版本号：`package.json` / `build.gradle` / 应用内「关于」三处一致（`npm run check:version` 锁死）；
- 单测：455 个用例全绿，`tsc --noEmit` 0 错误；
- APK SHA-256：932D749B52F69871251F0CF588DC37BDB0AFC13FAD100B1508DF83357B668539（9.21 MB，Windows Defender 扫描 `found no threats`）。
- 说明：上面的哈希对应本机签名的包；同一个 tag 由 Actions 重新打包时，Gradle 产出的 APK 不保证字节一致，以实际下载到的包为准（签名与版本号始终一致）。
- 发布资源名：GitHub 会把发布资源名里的非 ASCII 字符去掉（`课表助手-v1.4.0.apk` → `-v1.4.0.apk`），所以 Release 里的附件名用 ASCII 的 `timetable-app-v1.4.0.apk`；本机产出的文件名仍是中文的，两者是同一个包（哈希相同）。

---
## v1.3.0

走动像样了：慢下来 · 会翻面 · 走完停在原地

四条反馈，逐条落地。

**① 走路与休息要用不同的动画**

角色包的状态从四种变成**五种**，多了 `walk`：

```
idle 待机 / walk 走动 / react 被点一下 / sleep 长时间没动 / drag 被拖动时
```

素材解析多了一层判断（`assetKeyFor`）：相位是 idle、微动作是 walk、而且包里**确实有 walk 素材**时才用 walk，
否则沿用相位那套。**没有 walk 素材的角色也不会出错** —— 退回 idle，
并由一套程序化的「迈步」动作（`mascotGaitStep` 上下弹跳 + `mascotGaitTilt` 左右交替轻倾）把两者区分开；
有 walk 素材时**不叠加**，免得和素材自己打架（和「逐帧图不再叠加呼吸」是同一条理由）。

**② 速度再慢一点**

一趟从 5–9 秒改成 **11–20 秒**，位移 24–72px —— 折算下来 2–5 px/秒，是踱步而不是被平移。

**③ 允许偏离原位**

模型从「来回走回原点」改成「**家 + 漂移**」：

```
家（home）  = 用户放它的地方，只有拖动才变
漂移(drift) = 在家的附近累积的位移，走完停在那边，不回家
```

两个约束同时成立，都在 `placement.ts` 里算死：

| 约束 | 做法 |
| --- | --- |
| 不越过边界 | 落点 ∈ [edge − homeX, viewW − edge − homeX] |
| 不离家太远 | 落点 ∈ [−maxDrift, +maxDrift]（默认 96px，约 0.7 个身位）|

两者的**交集**就是这一趟能去的区间（`walkRoom`）；区间不足 8px 时返回 null，
状态机那边直接把 walk 的权重清零 —— **不是"走了但看不见"，而是根本不会走**。

还有一个细节：方向带**回归**倾向（已经偏出 24px 就更可能往回走），
所以它在家附近来回逛，而不会在几小时内随机游走到屏幕边缘定居。

**④ 反向走路要镜面对称**

`transform: translate(-50%, -100%) scaleX(-1)` —— 往左走时水平翻面。
约定是**默认朝右**，所以素材本身朝左的用户可以在外观 → 角色里勾「素材本身朝左」，把基准反过来。

**⑤ 长按菜单美化**

从「四个裸文字按钮」改成一张小卡片：角色名 + 每项配图标与一句说明（Feather 风格的 `target` 靶心图标是为这一版新加的），
底部一个指向角色的小三角；角色停靠在屏幕上半部分时自动改成往下弹。

## 验证（真实时间）

| 场景（95–110 秒，`?mascot=6` 带 walk 素材） | 结果 |
| --- | --- |
| 走动与漂移 | 走动采样 **152/475**（32% 的时间在走）、锚点 x∈[252,285]（漂出去并留在那儿）、**锚点越界=否** |
| 用哪张素材 | **走动时素材=walk**；迈步动作采样 **0**（有走路素材就不叠加程序化动作）|
| 镜像 | **翻面 64 / 正常 88** —— 两次方向都出现且各自翻面正确 |

## 过程中修掉的一个真 bug

第一次验证时发现「走动完全没动」：100 秒里锚点只变了 4px（那 4px 还是呼吸缩放带出来的）。
原因是这一趟的落点存在了 **ref** 里 —— ref 改了不会触发重渲染，于是新的 `translate` 目标永远写不进 DOM。
改成 state 之后才真的走起来。**「改动视图状态的东西不能用 ref」**，这条值得记住。

| 项目 | 结果 |
| --- | --- |
| 单元测试 | **455 通过**（摆放规则 29 条含穷举边界、状态机 50 条含素材解析）|
| 版本 | versionCode **49** / versionName **1.3.0** |
| 安装包 | `课表助手-v1.3.0.apk` · 9.2 MB |
| SHA-256 | `05E7DCC494D94F65F87A2D3E054B19FE8F7D1C27296F54D9C6090FC308F4BA1F` |
| 安全扫描 | Windows Defender 实扫无威胁 |

---

## v1.2.0

角色会在原地来回走走

**做法：把"家"和"走位"分开**

角色的位置是**归一化锚点**（存盘的 `prefs.mascot.x/y`）。走动如果直接改它，会有两个后果：
走走停停会把存盘位置带跑（走十次它就不在你放的地方了），而且每帧都要写盘。
所以走动做成**围绕家的临时位移**，只动 `translate`：

```
视觉位置 = 家（存盘值，不变） + 走动位移（-r … +r … 0）
```

CSS 动画的**起止位移都是 0**，所以动作开始和结束时都不会"啪"地跳一下，走完正好回到原位：

```css
@keyframes mascotWalk {
  0%   { translate: 0 0; }
  25%  { translate: var(--walk-r, 0px) 0; }   /* 往右 */
  50%  { translate: 0 0; }                    /* 回中 */
  75%  { translate: calc(-1 * var(--walk-r, 0px)) 0; }  /* 往左 */
  100% { translate: 0 0; }                    /* 回中，收尾 */
}
```

**半径怎么算（"不越过边界"的判据）**

`placement.ts` 里新增一个纯函数：

```ts
walkRadius(anchorX, viewW, edge, maxR)
  = min(maxR, anchorX - edge, viewW - edge - anchorX)
```

两侧哪边剩得少就按哪边 —— 于是：

| 站位 | 结果 |
| --- | --- |
| 屏幕中央 | 走满设定的半径（角色宽度的 0.5 倍，上限 64px）|
| 贴着右边 | 只能往左走 |
| 剩余空间 < 8px | 返回 0，**干脆不走**（几像素的位移看起来像抖动，不像走路）|

返回 0 时会同时把状态机里 `walk` 的权重清零（`allowWalk: false`）——
**不是"走了但看不见"，而是根本不会被掷到这个动作**。

**打断的处理（这次验证时发现的）**

用 `?poke=10` 做验证时发现：走动中途被点一下，走动动画被摘掉，位移**瞬间归零** ——
角色"啪"地跳回家里（最多 64px）。修法是给 `.mascot` 加一条 `transition: translate 260ms`：
动画运行期间 transition 不生效，只有被摘掉时才起作用，于是变成"走回去"。

拖动也要处理：手指按下的一瞬间走动会停，如果起点还按**家**算，角色会先跳回家再跟着手指走。
所以按下时读一次 `getComputedStyle(el).translate`，把这点偏移补进起点 —— 抓起正在走的角色，它原地不动地跟着手指走。

## 验证（真实时间，不是虚拟时钟）

新增 `?mpos=x,y`（放到指定锚点）与 `?behavecheck=秒数` 里的**锚点越界检查**：

> 判据是**锚点**（底部中心），不是整个身体 —— 身体允许挂在屏幕外（那是 v0.15.5 明确要的），
> 会出问题的是"落脚点跑出可用区域"。第一版检查量的是身体，结果在贴边用例上误报"越界"，已修正。

| 场景 | 结果 |
| --- | --- |
| 居中站（`?mpos=0.5`），85 秒 | **锚点 x∈[188,316]**（家 252 ± 64 = 算出来的半径）、可用区[6,498]、**锚点越界=否**、走动采样 101/425 |
| 贴右边站（`?mpos=0.985`，右侧只剩 1.6px），35 秒 | **走动采样 0/175**（一次都没走）、锚点 x∈[496,496]、锚点越界=否（本体 x∈[444,549] 挂在屏幕外，符合预期）|

| 项目 | 结果 |
| --- | --- |
| 单元测试 | **445 通过**（摆放规则 15 → 24 条，状态机 40 → 46 条）|
| 版本 | versionCode **48** / versionName **1.2.0** |
| 安装包 | `课表助手-v1.2.0.apk` · 9.2 MB |
| SHA-256 | `050BE07AD4D9DE9B91633C17C7B4490F1E4A14E730968D5F891FEB7574470133` |
| 安全扫描 | Windows Defender 实扫无威胁；走动关键帧与减弱动态效果规则均已在包内确认 |

---

## v1.1.0

角色更像个活物了

**问题**：状态机只有四个相位（idle / react / sleep / drag），而 idle 里只有两条固定周期的正弦动画
（呼吸 5.2s、浮动 6.8s，永远同相位、同幅度）。**看三秒就知道它在循环。**
更糟的是"随机"是假的：眨眼间隔由 `blinkDelay(seed)` 算，seed 每次 +1.37 ——
算式写得像随机，实际是**确定性**的，每个用户、每次启动看到的节奏完全一样。

**改法：相位之外再加一层「微动作」**

| 层 | 内容 | 谁决定 |
| --- | --- | --- |
| 相位（外层） | idle / react / sleep / drag | 用户操作 + 时间（语义稳定，不随机）|
| 微动作（内层） | 呼吸 / 抖一下 / 伸展 / 抬头 / 打哈欠 | **当场掷**：做什么、做多久、快慢幅度 |

- 每次动作从对应区间里随机取时长（呼吸 5.2–13s、抖一下 0.6–1.5s、伸展 2.2–3.8s…），
  再叠一层 ±15% 的"抖动"到周期与幅度上 —— 同样的"呼吸"，周期不会是永远 5.2 秒；
- **不会连着两次做同一个动作**：这是硬保证（掷到重复就重掷一次），不是"降权"
  （只降权的话单测里 300 次就能复现连续重复）；
- 眨眼改成真随机（`blinkDelayFor(Math.random())`）—— 随机源从状态机里**挪到调用方**，
  状态机里一行 `Math.random` 都没有。

**「精神头」：让性格能被感觉到**

`energyOf(hour, minutesToClass, idleForMs, interactions)` 算出 0..1 的能量，
它只影响内层（相位该怎样还怎样）：

| 场景 | 能量 | 表现 |
| --- | --- | --- |
| 临近上课（≤10 分钟） | +0.25 | 活泼、动作快 |
| 白天 / 清晨 | +0.08 ~ +0.15 | 正常 |
| 午后 / 深夜 | −0.15 / −0.35 | 蔫、打哈欠多 |
| 长时间没人理（>3 / >10 分钟） | −0.15 / −0.25 | 更蔫 |
| 刚被点过（最近 3 分钟） | 每次 +0.06，上限 +0.24 | 短暂活泼 |

**反应：四种变体，而且任何素材都看得见**

`hop`（蹦一下）/ `sway`（晃两下）/ `startle`（被吓一跳）/ `peek`（凑过来看），
每次点一下随机挑一个、且不连着重复。它们**只动 transform / opacity**，
所以用视频做的逐帧图角色也有反应 —— 这是以前完全没有的（以前点一下只有相位变了，画面不动）。

> 实现细节：同类反应连着两次时，React 认为 className 没变、CSS 动画不会重播。
> 所以用命令式地"摘掉类名 → 强制重排 → 加回类名"来重启动画（`MascotArt` 里的 innerRef 那段）。

**省电：从轮询改成排程**

原来是 `setInterval(…, 400)` 每 400ms 推进一次状态机 —— 一秒被叫醒 2.5 次，多数时候什么都没发生，
而且所有动作只能落在 400ms 的整数倍上（隐约的机械感）。现在 `nextWakeAt()` 算出下一个该变化的时刻，
用一个 `setTimeout` 排到那一刻；空闲时真的不醒。

## 验证：把"随机感"变成可测的量

新增 `?behavecheck=秒数`：每 200ms 读一次 `data-behavior` / `data-reaction`，
记录**切换序列**而不是逐次采样，最后报出"做了几种动作、各几次、最长一次持续多久、反应了几次"。
配 `?poke=N`（让它自己每 4 秒被点一下）就能验证反应路径 —— 无头浏览器里点不动它，
没有这个入口这条路径永远是"没验证过"。

实测（真实时间，不是虚拟时钟）：

| 场景 | 结果 |
| --- | --- |
| 60 秒安静待机 | 动作种类 **5**、切换 14 次、最长同动作 12.2s（`breathe`）、分布 `breathe×4 yawn×3 perk×4 stretch×1 fidget×3` |
| 45 秒 + 8 次点击 | 反应 **8** 次：`sway peek startle hop peek startle hop peek`（四种都出现、无相邻重复）；动作分布转向 `fidget×10` |

两个数字值得看：**动作种类 5/5**（旧版永远是 1），以及**被点之后 fidget 从 3 次涨到 10 次** ——
能量确实在影响行为，而不只是个装饰性参数。

## 顺带修掉一个真 bug（测试在周末失败暴露出来的）

跑全量测试时 `widget.test.ts` 红了两条。查下来不是测试写错，是**产品 bug**：

```ts
const today = todayISO();   // ← 真实的今天，而不是传入的 now
```

`buildWidgetPayload(data, now)` 忽略了它的 `now` 参数来做"今天"那一栏，
于是：调用方传别的时刻时，"今天"算的是真实当天；而那几条测试**只在工作日碰巧通过**
（它断言的其实不是它以为的那件事），一到周六就红。
现在改成 `toISODate(now)`，并补了一条专门的回归测试（同一时刻往前推一天，内容必须完全不同）。

| 项目 | 结果 |
| --- | --- |
| 单元测试 | **430 通过**（角色状态机 17 → 40 条）|
| 版本 | versionCode **47** / versionName **1.1.0** |
| 安装包 | `课表助手-v1.1.0.apk` · 9.2 MB |
| SHA-256 | `6E21760D0455A0FCE8AB910FD605FCF414BD1257EF023B5415E0F1539A9750E4` |
| 安全扫描 | Windows Defender 实扫无威胁 |

---

## v1.0.0 —— 首个公开版本

> 从这一版起，应用内的「更新日志」是**给用户看的发行说明**：只讲你能感知到什么变化。
> 逐版本的完整技术记录在 `CHANGELOG.md`，专题工程笔记在 `docs/`。

**交付**

| 项目 | 结果 |
| --- | --- |
| 版本 | versionCode **46** / versionName **1.0.0** |
| 单元测试 | **406 通过** |
| 布局自检 | 320 / 360 / 390 三档：OVERFLOW 0、SMALL_TAP 0、标签栏 5/5 在框内 |
| 安全扫描 | Windows Defender 实扫无威胁 |
| 签名 | 与历史版本同一把 key（可直接覆盖安装）|
| 安装包 | `课表助手-v1.0.0.apk` · 9.2 MB |
| SHA-256 | `6C54DA6B08959BD1B77FD203E7FF02DF57639947547CD0067E82E658A3089EC1` |

**这一版做了什么（产品化）**

功能上没有大改，改的是**面向用户的表达**：准备公开给同学使用，界面上不该再出现开发过程的口吻。

1. **应用内更新日志重写**：原来 0.12.0–0.15.6 的条目是写给我们俩看的（"你报的""我修的"
   "上一版只打了包没发出去"、调试参数、单测数量）。现在每一条都改成用户能感知的说法，
   并按版本整理成发行说明；0.11.0 及更早的条目同步收敛。
2. **术语统一**（把内部叫法换成用户叫法）：

   | 原来 | 现在 |
   | --- | --- |
   | 真机提醒自检 / 真机自检 | **提醒可靠性检查** |
   | 自检报告 / 自检进度 / 逐步自检 | 检查报告 / 检查进度 / 逐步检查 |
   | 调试模式 | **排查工具** |
   | 诊断信息 | **运行信息** |
   | 手滑 | 误触 |

   共 33 处，分布在设置、提醒可靠性检查、角色面板与说明书里。
3. **说明书与提示语去掉过程叙述**："这是最容易踩的坑""顺手压一遍""拖到不顺手的地方"
   这类写法改成中性说明。
4. **README 重写**成产品说明：给同学的安装步骤、功能总览、三条底线，开发内容归到「开发」一节。
5. **网页层补上 meta description 与 color-scheme**，`package.json` 补上项目描述。

**给同学的话（可以直接转发）**

> 课表助手的第一个正式版：本地优先的课表 App，不联网、无广告、不需要注册。
> 支持从教务系统的 Excel / CSV 导入课表，提醒走系统闹钟（息屏、关掉应用也照响），
> 外观可以完全自己调，还能放一个自己的角色在课表上。
> 数据只在你手机上，随时可以在「设置 → 课表数据」导出备份带走。

---

## v0.15.6

角色设置也纳入外观锁定

**问题**：外观锁定只挡住了 `button` / `input` / `switch` / `upload-zone` 这几种选择器，
而「角色」面板的动作**几乎全是可点行**：

```
导入角色包 · 更多功能 · 做一个角色 · 用一张图或一段视频 · 粘贴角色包内容
导出角色包 · 检查这个角色 · 放回默认位置 · 使用说明书 · 移除角色
```

于是外观明明锁着，这一整块照样点得动 —— 其中**「移除角色」还会顺手把素材从本机清掉**。

**改法**

- 锁定规则补上 `.list-row.tap`（以及 `[role="button"]`），角色面板的 9 行动作一并锁住；
- 角色面板上方新增一条**锁定提示条**，自带「解锁」按钮。
  原来只有顶部那条吸顶的锁定栏 —— 面板多的时候它早滚出屏幕了，
  **"锁住了却找不到钥匙"比不锁更糟**；
- 「移除角色」补上二次确认（设置 → 防误触可关掉）；
- 唯一保留的例外：**还没有角色**时不锁那一块。外观锁定防的是"手滑改坏已有的东西"，
  一个角色都还没有时，唯一的动作就是"第一次把它放进来"，不该被拦住。

**验证（自检新增一项）**

一个"锁了但没锁全"的机制，光靠看是看不出来的，只能量。自检现在会数
「锁定时仍可点的面板控件」和「解锁入口」：

| 场景 | 锁定时仍可点的面板控件 | 解锁入口 |
| --- | --- | --- |
| 有角色（`?mascot=4`） | **0** ✓ | 2 个，都可用 ✓ |
| 没有角色 | 2（导入角色包 / 更多功能，故意放行）✓ | 1 个，可用 ✓ |

单元测试 **406 通过**；布局自检 OVERFLOW 0、SMALL_TAP 0。

---

## v0.15.5

角色动得更顺 · 能拖的范围更大 · 不再出现在外观和设置页

**帧率**

- 视频抽帧的默认帧率 **12fps → 18fps**（帧数上限 48 → 60）。1 秒的素材从 12 帧变 18 帧，
  动作明显更顺；单格尺寸仍由「总像素不超过 800 万」那道闸门兜着，不会因此糊掉。
  回归测试里那条「常见竖屏短视频的单格高度 ≥ 490」在 18fps 下依然通过。
- 新增**「播放帧率」滑杆**（外观 → 角色，`0` = 跟随素材）。
  面板上把话说清楚了：**调高只会播得更快，不会更顺** —— 画面里的帧就那么多，
  想真正更顺得重新导入那段视频。做成偏好而不是改角色包，是因为"素材怎么抽的"和
  "我看的时候想多快"是两件事，导出给别人时不该带上我的速度。

**可移动范围**

原来的判据是「**整个角色**都要在安全区里」：左右各让出半个身位、上下各让出一个整身高。
一只 140px 高的角色，在 660px 的可用高度里只剩 **448px** 的纵向余地，横向更是被挤到中间一小条。

现在只要求「**落脚点**（底部中心锚点）在安全区内」：

| | 旧 | 新 |
| --- | --- | --- |
| 纵向余地（140px 角色 / 660px 可用） | 448px | **588px** |
| 横向 | 两侧各让出半个身位 | 几乎整屏 |
| 角色本体 | 必须完整在安全区内 | 可以一半挂在屏幕外 |

换句话说：**只剩落脚点在屏幕里也能拖到那里**，而落脚点还在，就一定拖得回来。
「放回默认位置」同时提到了面板主区域（长按角色的快捷菜单里一直有）。

顺带修掉两处：`clampAnchor` 遇到脏数据（NaN / Infinity）时会算出 NaN，现在回落到居中；
默认位置 0.86 → **0.82**（新判据下 0.86 会让角色贴出右边缘几个像素）。

**不再出现在外观页和设置页**

其实这个要求上一轮就提过一次（「桌宠只有在"本周""今日""任务"界面出现」），
当时我只做到了"在外观页让它穿透、不响应点击"—— 位置还在，视觉上还是杵在那儿。
这一版直接**从渲染上掐掉**：只在这三个课表页渲染它。
那条 `passive` 穿透规则随之成了死代码，一并删除。

> 说明一下我理解的边界：外观页的「角色」面板里那个**实时预览**（以及编辑器里的大预览）
> 保留 —— 那是"看清自己在调什么"的工具，不是一个到处乱跑的桌宠。
> 如果你连预览也不想要，说一声，去掉很快。

**验证**

| 项目 | 结果 |
| --- | --- |
| 摆放规则单测 | **15 条**（新增"能拖多远"的量测：纵向余地 > 500px、边界贴边、幂等、脏数据兜底）|
| 抽帧单测 | **32 条**（18fps 默认、帧数上限 60、单格高度仍 ≥ 490）|
| 外观 / 设置页 | 角色不渲染（自检报 `MASCOT none`）|
| 布局 320/360/390 | OVERFLOW 0、SMALL_TAP 0、顶层=yes、遮挡=0 |
| 单元测试 | **405 通过** |

---

## v0.15.4

角色一闪一闪的真正原因：逐帧图的定位算错了

> 你报了两轮「一闪一闪、大部分时间不显示」，我上一版修的是**帧数**（网格 16 格只画了 9 帧，
> 多出来的格子是透明的）—— 那确实是个真 bug，但**不是主因**。真正的主因在下面，
> 它从逐帧图这个功能上线（v0.12.0）起就一直在，而且靠看代码、量元素几何都发现不了。

**根因：CSS 百分比位移的语义**

逐帧图原来是用背景图做的精灵动画：

```css
background-size: 800% 200%;              /* 整张图缩到 8×2 倍容器 */
background-position: -100% 0%;           /* 想「往左推一格」，实际不是 */
```

`background-position` 的百分比**不是**按容器尺寸算的，而是按「容器尺寸 − 背景图尺寸」算：

```
位移 = 百分比 × (容器宽 − 背景图宽) = (-1) × (W − 8W) = +7W
```

也就是 `-100%` 把整张雪碧图**推到容器右边外面**去了 —— 只有第 0 帧（`0%`）能看见，
其余帧全是空白。**8×2 的网格里角色只出现 1/16 的时间，9 帧的网格里 1/9。**

### 证据（不用信我，直接看）

做了一张 4 格雪碧图（红、绿、蓝、黄），容器 40px，两种写法各排一排：

| 写法 | 四格实际显示 |
| --- | --- |
| `0% / -100% / -200% / -300%`（原来这么写） | **红、空白、空白、空白** |
| `0% / 33.3% / 66.7% / 100%` | 红、绿、蓝、黄 ✓ |

这一版的修法不是「换个百分比公式」，而是**彻底不用百分比**：

- 外层一个正好一格大的容器，`overflow: hidden`；
- 内层整张雪碧图按**像素**平移（`transform: translate3d(-col·W, -row·H, 0)`）；
- 位移计算抽成纯函数 `src/mascot/sheet.ts`，8 条单测钉死「每一帧都落在网格内、互不重复」。

顺带的好处：换帧变成合成层变换，**不触发重绘**。以前每换一帧都要重绘一次背景层，
手机上本来就容易掉帧。

**帧率：把 setInterval 换成 rAF + 时间累加**

你说「疑似帧率不对」，这个直觉是对的 —— 但来源有两层：

1. 上面那个 bug 让角色每圈只出现一帧，看起来就是「卡顿 / 闪烁」；
2. `setInterval` 是按排队的次数走的：主线程一忙（课表、提醒、资产解码都在同一线程），
   回调就会积压，恢复时连着触发好几次 —— 表现是「卡一下然后猛跳几帧」。

现在用 `requestAnimationFrame` + 时间累加：每帧按**真实经过的时间**算该显示第几帧，
忙的时候自动少跳（一次最多补 1 帧），闲的时候自动跟上，永远不会积压。

**逐帧自检：把「每一帧都真的画了东西」也测掉**

上一轮的 `?framecheck=1` 只验证了「露出来的是不是该露的那一格」，
而这次的问题恰恰是**位置没错、内容全空** —— 所以它测不出来。现在两件事分开测：

- `?framecheck=1`：从**真实几何**反推露出来的是哪一格，对比帧号，报 `misaligned`；
- `?frame=N`：把第 N 帧冻住，逐帧截图数像素。

实测（8×2 网格、9 帧）：**9 帧每一帧都画出了角色**（约 1130 个角色像素 + 2150 个白色像素），
而且帧与帧之间眼睛的位置不同 —— 确实是 9 个不同的帧，不是同一帧重复。

**封面更新**

- 用新版 `123jie.png` 重新生成全部图标与启动图（26 张 + 6 张预览）。
- 生成器顺带修掉两处：封面小于 992 时不再「先放大再缩回」（520 的封面白糊一次），
  非正方形封面改为居中裁剪，而不是拉伸变形。
- 图标底色仍是 `#2F3237`。另外生成了 `shots/icon-appearance-options.png`：
  三种做法并排对照（当前垫底 / 取封面外圈色垫底 / 封面满幅），想换说一声。

> 0.15.3 只打了包、没有发出去：打包过程中查出了上面那个更严重的问题，所以直接跳到 0.15.4。

**验证**

| 项目 | 结果 |
| --- | --- |
| 逐帧几何（实时渲染 3.2s） | `visited=9`（正好是真帧）、`misaligned=0`、`blankShare=0%`、PASS |
| 逐帧像素（9 帧逐帧截图） | 每帧都有角色（1130±20 深色像素）|
| 布局自检 320/360/390 | OVERFLOW 0、SMALL_TAP 0、顶层=yes、遮挡=0、标签栏 5/5 在框内 |
| 单元测试 | **402 通过**（新增 8 条：格子定位与像素位移）|

---

## v0.15.2

角色不再一闪一闪 · 素材不再被压糊

> 你报的两个现象（**一闪一闪、大部分时间不显示** 和 **分辨率太低**）不是两个 bug 的巧合，
> 它们是同一条管线上的两处硬伤，都出在「视频 → 逐帧图」这一步。下面是量出来的数，不是推测。

**修复**

- **角色一闪一闪、大部分时间不显示** —— 逐帧图的网格最后一排常常排不满：
  9 帧放进 8×2 的网格，就有 **7 个格子是全透明的**。而播放按「网格格数」（16）循环，
  于是每转一圈都会播到那 7 个空格子，**角色每圈凭空消失 7 帧**。
  按 12 fps 算，每循环有 0.58 秒什么都不显示；0.75 秒的短片（很常见）里空白占整个循环的 **44%**。
  抽样统计：0.3–8 秒的时长里，**37.9%** 会踩到这个问题。

  现在角色包里带上了 **frames（真实帧数）**，播放只走这几帧，空格子永远走不到。

- **已经做好的角色也会被自动修好**：启动后量一遍逐帧图里真正有几帧，需要就写回角色包
  （只收紧、不放大；量不到就什么都不做）。**不用重新导入**。

- **分辨率太低** —— 抽帧上限原来是「整图 2048 / 单格 480」。1080p 竖屏视频抽 48 帧之后
  每格只剩 **183×326**，而显示高度 140px 在 3.5 倍屏上需要 **490** 物理像素 ——
  放大 1.5 倍，肉眼就是糊。现在上限是 **整图 4096 / 单格 720 / 总量 800 万像素**，
  并且优先选能**整除**帧数的列数（12 帧排 6×2、18 帧排 6×3、24 帧排 8×3），每格都排满。
  实测同一个 500×700 的素材：单格从 233×326 提到 457×640，已经接近原始像素。

**新增**

- **「检查这个角色」**（外观 → 角色 → 更多功能）：量出每帧多少像素、网格几格、实测几帧、
  会被放大几倍，并直接告诉你显示高度调到多少最清楚。**糊不糊是可以算出来的**，不该让你对着屏幕猜。
- 编辑器新增 **「共几帧」** 与 **「自动数帧」**：手填格数时应用会自己数一遍图里真正有几帧，
  只在数出来比格数少的时候才改。
- **导入图片不再「顺手压一遍」**：不超过 3MB、400 万像素（动图 150 万）就原样使用 ——
  没有二次有损压缩，动图也不会被 canvas 拍成静态图（以前导入 GIF / 动图会变成不动的）。
  真要压时的长边上限从 1024 提到 1536。

**优化**

- 抽帧等到**真正出帧**再画（requestVideoFrameCallback），避免解码器慢半拍时抽到空白或重复画面。
- 导入视频后把实测结果留在面板上（帧数、单格尺寸、够不够清楚、有没有空帧），
  而不是弹一个转瞬即逝的提示。
- 切状态不再闪白：四个状态的素材在角色装上时就先解码好；单格比例未知的瞬间用上一次已知的比例顶着。
- 编辑器的大预览原来**只有类名、没有样式**，一直是个没边界也没棋盘格底的空框，看起来像排版坏了；
  现在是一块正经的预览台。

**验证**

- 新增自检 **`?framecheck=1` 逐帧播放检查**：每 40ms 读一次精灵元素的 background-position，
  折回格号后判断「这一步有没有走到透明格子上」，结论直接回传出来。
  在无头浏览器里对**同一个素材**做了 A/B：

  | 素材 | 包里有 frames | 访问到的格子 | 落在空格子上的比例 | 结论 |
  |---|---|---|---|---|
  | 8×2 网格 + 只画 9 帧 | 否（= 旧行为） | 13/16 | **31%** | FAIL |
  | 同一个素材 | 是（9） | 9（正好是真帧） | 0% | PASS |
  | 4×2 满网格 | 是（8） | 8 | 0% | PASS |

- 布局自检 320/360/390 三档 + 角色编辑器 + 外观页：OVERFLOW 0、SMALL_TAP 0、
  `顶层=yes`、`遮挡可点控件=0`、应用壳完好。
- 单元测试 354 → **394**（新增帧数校验、排版分辨率、空帧判定、分辨率建议、原图保留判据）。

---

## v0.15.1

图层加固：角色不会再被盖住

> **一个流程失误先说清楚**：我在同一个版本号（0.15.0）下先后发了两个不同的包，
> 第一个有下面说的类名撞车问题。所以你手机上装的可能是坏的那个。
> 这一版升到 **0.15.1 / versionCode 40**，就是为了一眼能分清装的哪份。

**修复**

- **角色可能被页面内容盖住**：图层层级从 25 提到 55（高于悬浮按钮、低于所有弹层）。
  25 在数值上已经高于静态内容，但页面上任何冒出 30~50 的元素都会把它压住 ——
  而"被盖住"在手机上看起来就是"角色不见了"。
- **默认位置会压住「今日」页出勤按钮的最后一个**（到/迟/缺/假 那一行排到 x249，
  角色盒从 x228 开始）。默认改成贴右侧安全区，七个场景实测不再遮挡任何控件。

**优化**

- 自检新增 **「角色是否画在最上面」** 一项。原来只测几何遮挡 ——
  而"位置正常却看不见"这类问题（正是类名撞车那次的症状）恰恰测不出来。
  判定方式是临时的：把 pointer-events 打开问一次 elementFromPoint 再还原。

**验证**

- 五个标签页 + 逐帧图 + 动图，共 7 个场景 × 320/360/390 三档：
  `顶层=yes`、`遮挡可点控件=0`、OVERFLOW 0、SMALL_TAP 0、应用壳完好。

---

## v0.15.0

视频当角色 · 粘贴导入 · 选素材界面重做

**新增**

- **视频也能当角色**：选 webm / mp4，应用会自动抽帧拼成逐帧动图。
  二游的动图素材大量是 webm，而它是视频不是图片 —— 以前会被直接拒掉。
  默认抽 12 帧/秒、最多 48 帧，取每小段的中点（避开很多视频开头的黑帧）。
- **粘贴角色包**：把内容整段粘进来即可。手机只给了相册权限、
  文件管理器里找不到 .json 时，这条是唯一能走通的路。

**修复**

- **选择器以前只接受图片**：安卓的相册选择器按 MIME 过滤，只写 `image/*` 的话，
  相册里的视频根本不会出现在待选列表里 —— 表现就是"想选那个视频，列表里压根没有它"。
  现在同时接受图片与视频。

**优化（界面）**

- **选素材界面重做**：四个状态排成缩略图网格，**点一格编一格**，下面只显示这一格的设置。
  原来是四个状态各堆一坨同样的控件（选图 + 形态 + 三个数字框 ×4），又长又难扫。
- **缩略图带棋盘格底**：角色素材多半是透明镂空的，放在纯色底上根本看不出哪里是空的。
  逐帧图只显示第一格并标出格数，动图标一个"动"字。
- **外观页的角色面板**：左边一个**活的**角色预览（和桌面上同一个渲染组件），
  入口改成列表行 —— 和全应用里"点进去做点什么"的写法一致，不再是五个按钮挤成一行还换行。
- **在外观页里角色自动穿透**：那一页是专门用来调它的，屏幕上又全是控件，
  角色正好压住某一个时用户会以为按钮坏了。现在它只剩观赏作用，回课表页立刻恢复。

**修复（这一版的第二批）**

- **角色被拉伸**：缩略图的修饰类名和「底部弹层」的全局类名**撞了**（都叫 `sheet`），
  那张小图被当成全屏固定盒子渲染 —— 逐帧图于是铺满整屏，看起来就是角色被压扁。
  类名撞车不会有任何报错，是靠量盒子尺寸发现的。现在所有自定义类都带 `mascot-` 前缀。
- **选完素材找不到保存**：保存按钮原来在全屏弹层的最底部，而且按钮文字会被
  「至少要给「待机」选一张图」这句提示顶替 —— 在别的格子上选了图，就既看不到「保存」，
  也想不到要往下滚。现在**「保存」和「导出」固定在右上角**，一直看得见，缺什么也写在旁边。

**界面收敛**

- **角色面板只留一个常驻入口**：`导入角色包`。其余（做一个 / 编辑 / 用图或视频 /
  粘贴 / 导出 / 放回默认位置 / 教学 / 移除）全部收进 `更多功能`，默认收起。
- 四个状态的缩略格改成**纯展示**，不再是一排可点按钮。

**验证**

- 槽位网格在 320px 窄屏上不再横向溢出（网格项默认 `min-width:auto` 会被内容撑破，
  改成 `minmax(0, 1fr)` 并给格子加 `min-width: 0`）。
- 四个界面 × 320/360/390：OVERFLOW 0、SMALL_TAP 0、应用壳完好；
  外观页 `遮挡可点控件=0（当前穿透）`，课表页角色照常可交互。
- `tsc --noEmit` 0 错；单元测试 **354 项全绿**（新增视频抽帧排版 14 项）。

---

## v0.14.0

内置角色编辑器 + 修掉一处白屏

**新增**

- **内置「做一个角色」编辑器**：外观 → 角色 → 做一个角色。选图、选形态（静态图 / 动图 / 逐帧图）、
  填格数与帧率、拉动作滑杆，**上面有实时预览**（和桌面上是同一段渲染代码），
  填完「保存并应用」直接生效，「导出角色包」拿走一个 .json 发给别人。
  已经导入的角色也能「编辑这个角色」再改 —— 不用再手写文件。
- **错误围栏**：万一再出现渲染错误，界面上会显示一张能读的错误卡片
  （重试 / 复制错误信息 / 一键移除角色），而不是一片白。
  角色是装饰，它出问题不会再拖垮整个应用。

**修复**

- **修掉一处白屏**：点「暂时收起来」或「移除角色」会让整个应用变白，只能卸载重装。
  根因是一个**条件执行的 hook** —— 角色可见时跑 N+1 个 hook、隐藏时只跑 N 个，
  React 判定数量不一致（error #300）就卸载整棵树。
  `tsc` 抓不到、单测抓不到、布局自检也抓不到，**只有真的点下去才会炸**。

**优化**

- 自检新增 **ROOT 指标**：应用壳被清空会明确报出来。
- 自检新增 `?mascotop=hide|remove|size|editor` 操作参数，把
  "隐藏 / 移除 / 改尺寸 / 开编辑器"这几条路径纳入无人值守检查 —— 这次的 bug 恰好只在这几条路径上出现。
- 编辑器把"表单 → 角色包"抽成了纯函数（`src/mascot/draft.ts`），16 个用例锁住语义，
  并且**复用导入用的那套校验**收敛数值，所以"自己做出来的包"和"导入的包"在运行时是同一种东西。

**验证**

- 用同源驱动脚本对**真实构建产物**逐项点击：隐藏 → 恢复 → 编辑 → 移除 → 新建，
  **控制台错误数 0，应用壳始终在**（修复前隐藏那一步 root 直接变 0）。
- `tsc --noEmit` 0 错；单元测试 **340 项全绿**（新增草稿层 16 项）。

---

## v0.13.0

角色的第二轮打磨：更顺手，也更不容易出错

**新增**

- **长按角色弹出快捷菜单**：暂时收起来 / 回到默认位置 / 打开外观设置 / 看教学。
  长按 0.6 秒，和课程卡长按是同一个约定，不用学第二套。
- **角色会跟着课表有作息**（纯规则，不涉及任何模型）：
  临近上课的十分钟里它会时不时招手；今天的课上完了又已经入夜，或者深夜凌晨，
  它打瞌睡的阈值会从 3 分钟缩到 45 秒。

**修复**

- **逐帧雪碧图角色根本显示不出来**：容器宽度塌成了 0。
  `background-image` 的元素没有固有尺寸，`width:auto` 会算成 0 —— 这个模式此前
  从来没有被运行时验证过。这轮加了 `?mascot=2` 自检入口，第一次跑就报出了 `MASCOT 0x140`。
- **换角色时旧素材残留在本机**：现在按"旧包用到的 key 减去新包用到的 key"精确回收。
  刻意不做全局扫描式清理 —— 那需要一个正确的 keep 列表，算错一次删掉的就是还在用的图。

**优化**

- **角色只在安全区里活动**：上不钻顶栏、下不压标签栏。这两个高度是**实测**的
  （安全区、横竖屏、分屏都会改），不是写死的常量。转屏 / 分屏 / 软键盘弹起之后
  会重新钳制一次，越界的位置被悄悄纠正 —— "拖到屏幕外找不回来"这类问题从结构上排除。
- **调大小的滑杆不再每动一下写一次盘**，松手 400ms 后才保存。
- **自检三条渲染路径**：`?mascot=1/2/3` 分别覆盖静态图、逐帧雪碧图、动图，
  位置、遮挡、素材形态、当前相位都会写进自检报告。

**验证**

- `tsc --noEmit` 0 错；单元测试 **324 项全绿**（新增 12 项覆盖位置钳制：顶栏/标签栏/横屏/角色比屏幕宽等边界）。
- 三种素材形态 × 320/360/390：OVERFLOW 0、SMALL_TAP 0、`MASCOT 105x140`、遮挡可点控件 0。

---

## v0.12.0

课表界面上的「角色」

**新增**

- **可以放一个角色在课表界面上**：会呼吸、会眨眼，点一下有反应，按住能拖着换位置，
  三分钟没人理会打瞌睡。位置会记住，大小可调，随时能收起来。
- **默认完全不显示** —— 导入一个角色包它才出现。应用不内置任何角色、也不提供下载渠道：
  素材从哪来是你自己的事，应用只负责在本机把它显示出来。
- **两种导入方式**：直接拿一张图当角色（静态图或动图都行，应用会自动压到合适大小），
  或者导入别人分享的角色包（.json）。
- **角色包支持三种素材形态**：静态图、动图（WebP / APNG / GIF）、逐帧雪碧图。
  全部是 WebView 原生就能播的，**不引入任何第三方运行时**。
- **说明书新增「角色」一章**：从怎么用讲到怎么自己做一个角色包，含字段说明表、
  可直接照抄的最小示例、素材准备建议与常见错误。

**优化**

- 角色素材存进应用内置数据库（和壁纸同一条管线），**不占本地存储配额**；
  移除角色时素材一起清掉，不会留下孤儿文件。
- 尊重系统的「减弱动态效果」：开启后角色只静态显示、不做任何自走动画；
  页面切到后台时计时器全部停掉。
- 角色浮层**不参与布局、不挡可点控件**：外层 pointer-events 为 none，
  只有角色本体可点，它飘在哪个按钮上面那个按钮仍然点得到。布局自检新增了这条检查。

**说明**

- 这一版**没有做 AI**。AI 在提醒、录入这些核心路径上要么不稳定、要么不如规则快，
  与其做一个能演示但没人天天用的东西，不如先把确定能做好的事做扎实。

**验证**

- `tsc --noEmit` 0 错；单元测试 **312 项全绿**（新增 37 项覆盖角色包校验、资产外置与还原、状态机）。
- 六个界面 × 320/360/390 的布局自检：没有角色时 `MASCOT none`（确认零内置），
  有角色时 **遮挡可点控件 = 0**、OVERFLOW 0、标签栏 5/5 在框内。

---

## v0.11.0

表格导入 · 小组件跳转 · 外观分享码 · 时区重排

**新增**

- **从 Excel / CSV 导入课表**：教务系统导出的表格直接读进来，不用一门一门手录。
  支持 .xlsx 与 .csv / .txt（自动识别 UTF-8 / GBK 与分隔符），自动认表头与列对应关系，
  预览确认后才写入，导入后提示条上可以一步撤销。
  两种表格形式都能识别：**一行一个时段**的长表，以及**行是节次、列是星期**的课表矩阵
  （格子里那几行"课程名 / 教师 / 周次 / 教室"会被逐项拆开）。
- **小组件点击直接进课程详情**：2×2 点卡片打开下一节课，4×2 每一行打开各自那门课
  （计划书 6.5 节的要求）。
- **外观分享码**（`KBT1.` 开头）：把配色、字体、排版这些参数分享给同学，粘贴即用。
  壁纸图片动辄几 MB，塞不进文本，仍然走主题包文件 —— 界面上写明了这一点。
- **真机自检可导出报告**：版本、构建时间、运行环境、权限与后台可靠性状态、
  8 项自检结果合成一份文本，通过系统分享面板发出去，排查时不用再逐条口述。

**修复**

- **改系统时间 / 换时区 / 应用更新后，提醒会重新排程。**
  在此之前，提醒是按改动前的绝对时刻排给系统闹钟的 —— 时间一改，闹钟照样准点响，
  但响在了错误的时间点上。现在原生侧监听 `TIME_SET` / `TIMEZONE_CHANGED` /
  `MY_PACKAGE_REPLACED` 并记下标记，应用回到前台时重排一次。
  （开机重排一直由通知插件自带的 RestoreReceiver 负责，不受影响。）

**优化**

- **触控目标补齐**：外观页有 18 处、导入向导与设置里有若干处控件只有 42px / 34px 高，
  低于全应用 44px 的标准。现在都靠伪元素外扩补到 44px，视觉尺寸一点没变。
- **无障碍**：底部标签栏、周次条、课程卡都加上了朗读标签（课程卡会念出
  "课程名、第几节、时间、地点、教师"），课程卡支持键盘 Enter / 空格打开，
  提示条带 `role=status` 会被屏幕阅读器念出来。

**验证**

- `tsc --noEmit` 0 错；单元测试 **275 项全绿**（新增 62 项覆盖表格解析、矩阵解析、外观分享码）。
- 六个界面 × 320/360/390 三档的布局自检，并与 v0.10.0 做逐项 A/B 对比。

---

## v0.10.0 — 2026-09-11

使用说明书 · 更新日志 · 修两个真 bug

**新增**

- 应用内「使用说明书」：17 个章节，从第一次打开讲到换机迁移。写作时不假设学校、学期与作息，任何场景都能照着用；每条都能在界面里找到对应的按钮。
- 应用内「更新日志」：入口在「设置 → 关于」，本文件与它内容一致。
- 首次启动的隐私说明页增加了「先看使用说明书」入口。

**修复**

- **退出重进后自定义背景丢失。**
  写入磁盘时，大图会被外置成 `asset:<key>` 引用（浏览器存储只有约 5 MB 配额，一张壁纸就能吃满），
  但启动校验只认内嵌的 `data:image/...`，于是在还原图片之前就把它判成无效并清空。
  启动路径是「同步读本地存储 → 再异步还原图片」，校验卡在中间，图片永远等不到还原那一步。
  现在校验同时接受引用形态，并补了把「写盘 → 冷启动 → 还原」整条链路走一遍的回归测试。
- **「导出主题包」点了没有反应。**
  浏览器里给隐藏的 `a` 标签设 `download` 再 `click()` 就能存文件，安卓 WebView 里没有下载宿主，这个点击会被直接丢掉。
  改为原生写文件（应用缓存目录，不需要任何存储权限）后调系统分享面板。
- 同一原因导致 **JSON 备份、ICS 日历、CSV / Markdown 导出** 也点了没反应，一并修好。
- 资产库里确实找不到壁纸图片时（清过应用数据、换过手机），外观页不再显示「已选自定义壁纸」却什么都看不到，
  而是明确回退成无壁纸并写回磁盘，下次启动就是干净状态。
- 导出过程不再可能「点了没反应」：原生调用全部加了时限（分享插件在面板已经打开时，
  第二次调用既不成功也不失败，会把按钮永久卡住），分享面板打不开或超时会自动把文件存进「文档」目录。

**优化**

- 导出结果按实际结果分三种说法：已打开分享面板 / 已保存到「文档」目录 / 已取消。
  原来是笼统的一句「导出成功」，用户找不到文件时会以为功能坏了。
- 导入主题包时，如果里面的壁纸是本机引用而不是内嵌图片，明确提示并跳过，不再留一张渲染不出来的壁纸。
- 导出主题包时若内存里仍是引用形态，明确按「无壁纸」打包，而不是往包里写一张空图。

**验证**

- `tsc --noEmit` 0 错；单元测试 207 项全绿（新增 8 项，覆盖壁纸跨重启与主题包打包）。
- 真机安装包与布局检查见 `docs/09-mobile-and-features.md`。

---

## v0.9.9

去掉 emoji，改用内联图标

- 清掉界面里 21 处 emoji，全部换成统一线宽的内联 SVG 图标，字形与颜色跟随主题。
- CSS 里承担状态语义的那处 emoji 换成图形，不再依赖系统字体里长什么样。

## v0.9.8

小组件修复 · 设置页瘦身

- 修复 2×2 小组件倒计时显示成 `496963:41:52`：计时器时基用错，改成系统开机计时。
- 修复 4×2 小组件一片空白：改用内联行渲染，不再依赖动态添加视图。
- 修复应用启动后才添加小组件时、必须重启应用才会同步的问题。
- 设置页只留常用项，检测类与诊断类内容收进「调试模式」。

## v0.9.7

图片资产库（SQLite）

- 壁纸与课程配图存进应用内置数据库，浏览器存储里只留轻量引用，不会再因为一张大图顶满配额。
- 老数据在升级后首次启动时自动迁移，不需要手动操作。

## v0.9.4 – v0.9.6

插件系统 · 桌面小组件

- 插件系统：插件只提供声明式数据，不执行任何代码；可启用、停用、授权、卸载。
- 桌面小组件 2×2（下一节课 + 倒计时）与 4×2（今天剩下的课）。
- CSV / Markdown 导出改由内置插件提供，接口与第三方插件完全一致。

## v0.9.0 – v0.9.3

计划扩展第一批

- 首次启动的隐私说明页。
- 四个通知渠道，可在系统设置里分别调节。
- 应用内的真机自检清单（8 项，逐条给出结论）。
- 全局撤销 / 重做，提示条上即可撤销上一步。
- 分享码：把整份课表压成一段文本，同学粘贴即可还原。
- GitHub Actions 自动构建 + 版本一致性检查。

## v0.8.x

打底版本

- 周课表、今日视图、任务、可无限延伸的时间轴。
- 设计令牌驱动的主题系统：预设、壁纸、主色、字号、密度、卡片质感。
- 系统闹钟提醒：课前多次提醒、按课程与按时段覆盖、每日摘要。
- ICS 日历导入导出、数据 JSON 备份、课表长图分享。