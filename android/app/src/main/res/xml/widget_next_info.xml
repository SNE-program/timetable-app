<?xml version="1.0" encoding="utf-8"?>
<!--
  小的那个小组件（下一节课 + 倒计时，拉高之后多列两节）。

  previewLayout / previewImage：选择器里的预览图（Android 12+ 用 layout，更老的用图标）。
  updatePeriodMillis=0：同另一个组件，靠应用推送与到点自刷新。
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="110dp"
    android:minHeight="110dp"
    android:targetCellWidth="2"
    android:targetCellHeight="2"
    android:minResizeWidth="110dp"
    android:minResizeHeight="110dp"
    android:maxResizeWidth="320dp"
    android:maxResizeHeight="460dp"
    android:updatePeriodMillis="0"
    android:initialLayout="@layout/widget_next"
    android:previewLayout="@layout/widget_next_preview"
    android:previewImage="@mipmap/ic_launcher"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:description="@string/widget_next_desc" />
